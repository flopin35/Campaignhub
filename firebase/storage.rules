rules_version = '2';

// MVP rules — authenticated users can read/write.
// TODO: restrict uploads by ownership and validate file metadata before production.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
